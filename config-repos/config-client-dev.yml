spring:
  application:
    name: config-client
   cloud:
    bus:
      trace:
        enabled: true
      enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7000/eureka/
server:
  port: 8088

  